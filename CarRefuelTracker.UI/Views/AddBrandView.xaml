<UserControl x:Class="CarRefuelTracker.UI.Views.AddBrandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarRefuelTracker.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Height="200" Width="370">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Fahrzeugmarke hinzufügen" FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <DockPanel Margin="20" VerticalAlignment="Top">
            <TextBlock Margin="0,0,15,0" VerticalAlignment="Center" Text="Fahrzeugmarke: "/>
            <TextBox Height="25" x:Name="BrandName"/>
            </DockPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="Cancel" Content="Abbrechen" Margin="0,0,15,0"/>
                <Button x:Name="AddBrand" Content="Speichern"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
